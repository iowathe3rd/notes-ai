# --- Database (async) ---
# Used by the application runtime (async engine, drivers like asyncpg).
DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/notes

# --- Database (sync) ---
# Used by Alembic migrations (sync engine, psycopg).
DATABASE_URL_SYNC=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/notes

# --- Kafka / Redpanda ---
KAFKA_BOOTSTRAP=127.0.0.1:19092

# --- Multi-tenant (MVP) ---
# If not set, API will generate a tenant_id for every request unless you provide X-Tenant-Id header.
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000001

# --- Docker Compose: Postgres container ---
# docker-compose.yml reads these when starting postgres.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=notes
